import { createAction } from '@ngrx/store';

export const increment = createAction('[Course] Increment');

export const decrement = createAction('[Course] Decrement');

export const reset = createAction('[Course] Reset');